# Honeycomb.io Configuration
# Get your API key from https://ui.honeycomb.io/account
HONEYCOMB_API_KEY=your_honeycomb_api_key_here

# Dataset name in Honeycomb (optional, defaults to 'docquery-loadgen')
HONEYCOMB_DATASET=docquery-loadgen

# Alternative: Use custom OTLP endpoint if not using Honeycomb
# If HONEYCOMB_API_KEY is not set, the load generator will fall back to
# the --otlp-endpoint CLI parameter or http://localhost:4317
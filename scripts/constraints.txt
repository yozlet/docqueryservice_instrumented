# Core dependencies with strict versions
requests==2.31.0
python-dotenv==1.0.1
sqlalchemy==2.0.27
psycopg2-binary==2.9.9

# Testing dependencies
pytest==8.0.0
pytest-mock==3.12.0
pytest-cov==4.1.0
responses==0.24.1

# Explicitly exclude conflicting packages
# Exclude flask-appbuilder as it's not directly used and causes dependency conflicts
flask-appbuilder>=4.6.3  # Required by some transitive dependency, but we don't use it directly
